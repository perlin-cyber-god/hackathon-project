# This code has syntax errors, unused variables, and bad practices.

import sys # Unused import

def bad_function(a b): # Syntax Error: missing comma
    x = 10 # Unused variable
    password = "hardcoded" # Security risk
    print("this line is way too long and it just keeps going on and on and on and on and on to violate the line length rule for pylint")
    if a > b
        return a # Syntax Error: missing colon
    else
        return b # Syntax Error: missing colon

result = bad_function(5, 3)
